# ProjectTemplateFrontendSpaReact

Реализация простой модульной архитектуры.

---

## 🏗 Архитектура

В основе проекта лежит **модульный подход**. Система разделена на независимые блоки.

### 📦 Модули
Модуль — это изолированная единица функционала (отдельный route или переиспользуемый блок) со своей внутренней бизнес-логикой.

* **Иерархия:** Если функционал связан общим роутом (например, `/auth`, `/auth/login`), дочерние роуты выносятся в отдельные подмодули.
* **Экспорт:** Модуль предоставляет свой основной компонент и собственный `reducer`.
* **State Management:** Редюсер модуля регистрируется в глобальном хранилище динамически в момент старта приложения.
* **Инкапсуляция:** Модуль полностью ничего не знает о системе роутинга. Навигация инициируются через `CustomEvent`.
* **Оптимизация:** Компоненты модулей рекомендуется оборачивать в `lazy` для эффективного разделения кода (code splitting).

### 📄 Страницы
Страница выполняет роль композитора, собирая один или несколько модулей в единое представление.

* **Роутинг:** Страница импортирует модули и определяет конфигурацию роутов.
* **Вложенность:** Отображение дочерних роутов реализуется через компонент `Outlet`.
* **Координация:** Страница слушает соответствующие `CustomEvent` от модулей и выполняет переключение роутов.

---

## 🛠 Инструменты и разработка

### Мок-сервер
Для имитации API используется: 
[ProjectTemplateFrontendSpaReact-mock-server](https://github.com/AlexeyAbretov/ProjectTemplateFrontendSpaReact-mock-server).

### Тестирование сценариев
Для работы в режиме разработки используется файл `develop.html` вместо стандартного `index.html`. 
* В нем переопределен `window.fetch`, который автоматически считывает ключ сценария из `localStorage`.
* Этот ключ передается в каждом запросе через заголовок `x-scenario`.

---

## 🚀 План развития (TODO)

- [ ] Инкапсуляция логики запросов: обернуть `fetch` в специализированный класс.
- [ ] ⏳ **Документация проекта**.
- [ ] Разработка расширения для VS Code для автоматической генерации структуры модулей.
- [ ] Реализация полноценной шины событий (`EventBus`).